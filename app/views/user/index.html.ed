<table border="1">
	<% foreach(User user ; controller._users) { %>
	<tr>
		<td><%= user.name() %></td>
		<td>
			<%=
				user.hide_email_address() ? "true" : "false";
			%>
		</td>
	</tr>
	<% } %>
</table>

<%= controller._things %>

